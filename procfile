web: daphne -b 0.0.0.0 -p 8000 diagram_backend.asgi:application

# Procfile dentro de /backend (el directorio que despliegas). Al publicar solo esta carpeta,
# la ruta del módulo ASGI es 'diagram_backend.asgi:application'.
# Si Azure ignorara este Procfile, usa el fallback startup.sh y configura STARTUP_COMMAND.
