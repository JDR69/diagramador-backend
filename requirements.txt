############################################
# Runtime Core
############################################
# Alinear con la versión realmente usada localmente (ver salida del servidor: Django 5.2.6)
django==5.2.6
djangorestframework
django-cors-headers

############################################
# Async / WebSockets (Channels + Redis layer)
############################################
channels
channels-redis
daphne  # Servidor ASGI para producción (reemplaza uso directo de gunicorn para websockets)

############################################
# Base de datos
############################################
psycopg[binary]  # Driver PostgreSQL (psycopg3); elimina necesidad de psycopg2-binary
dj-database-url

############################################
# Config / Utilidades
############################################
python-decouple
whitenoise
requests

############################################
# Servidor WSGI opcional (legacy HTTP only). Mantener si quieres fallback sin websockets.
gunicorn

############################################
# Empaquetado / build (generalmente ya vienen en entorno, pero explícitos por compatibilidad)
setuptools
wheel

############################################
# Dependencias indirectas fijadas solo si hace falta reproducibilidad estricta
# (Puedes ejecutar `pip freeze > requirements.lock.txt` para snapshot completo)



